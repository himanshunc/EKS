# Terraform local state and cache — never commit these
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
tfplan

# Local variable overrides — contain environment-specific secrets/values
*.tfvars.local
*.auto.tfvars

# Sensitive outputs
terraform.tfvars.json

# Claude Code — keep locally, never push to GitHub
CLAUDE.md
.claude/

# OS files
.DS_Store
Thumbs.db

# Editor directories
.idea/
.vscode/
*.swp
*.swo
