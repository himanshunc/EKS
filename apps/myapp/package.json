{
  "name": "myapp",
  "version": "1.0.0",
  "description": "Sample Node.js app deployed via Helm + ArgoCD",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test server.test.js",
    "lint": "eslint server.js server.test.js"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
