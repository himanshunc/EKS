myapp has been deployed!

Get the ALB URL:
  kubectl get ingress -n {{ .Release.Namespace }}

Check pods:
  kubectl get pods -n {{ .Release.Namespace }}

Check logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "myapp.name" . }}
